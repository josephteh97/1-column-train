"""FastAPI + OpenSeadragon correction reviewer for the column detector.

This package replaces the deleted `correct_detections.ipynb` notebook
with a single-purpose web reviewer. Entry point is
`scripts.correction_app.app.create_app(drawing_id, config)`.

Launch via `python3 scripts/hitl.py review <drawing-id>`.
"""
